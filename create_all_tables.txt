CREATE TABLE DEPARTMENT (

    DEPT_ID		INT				NOT NULL,

    DEPT_NAME	VARCHAR(255)	NOT NULL,

    CONSTRAINT DEP_PK1 PRIMARY KEY(DEPT_ID));



CREATE TABLE POSITION (

    POSITION_ID		INT				NOT NULL,

    POSITION_NAME	VARCHAR(255)	NOT NULL,

    CONSTRAINT POS_PK1 PRIMARY KEY(POSITION_ID));



CREATE TABLE JAPANESE_LEVEL(

    JPNLEVEL_ID		INT				NOT NULL,

    LEVEL_NAME		VARCHAR(255)	NOT NULL,

    CONSTRAINT JAP_PK1 PRIMARY KEY(LEVEL_ID));



CREATE TABLE AUTHORITY(

    AUTHORITY_ID	INT				NOT NULL,

    AUTHORITY_NAME	VARCHAR(255)	NOT NULL,

    CONSTRAINT AUT_PK1 PRIMARY KEY(AUTHORITY_ID));



CREATE TABLE EMPLOYEE (

    EMP_ID			CHAR(10)		NOT NULL,

    EMP_NAME		VARCHAR(255)	NOT NULL,
	
	EMP_PASSWORD	VARCHAR(8)		NOT NULL,

    BIRTH_DATE		DATE			NOT NULL,

    GENDER			CHAR(1)			NOT NULL,

    MARTIAL_STATUS	CHAR(1)			NOT NULL,

    JPNLEVEL_ID		INT				NOT NULL,

    DEPT_ID			INT				NOT NULL,

    JOIN_DATE		DATE			NOT NULL,

    POSITION_ID		INT				NOT NULL,

    EMAIL			VARCHAR(255)	NOT NULL,

    PHONE			VARCHAR(15)		NOT NULL,

    CONSTRAINT EMP_PK1 PRIMARY KEY(EMP_ID),

       FOREIGN KEY (DEPT_ID) REFERENCES DEPARTMENT(DEPT_ID),

       FOREIGN KEY (JPNLEVEL_ID) REFERENCES JAPANESE_LEVEL(JPNLEVEL_ID),

       FOREIGN KEY (POSITION_ID) REFERENCES POSITION(POSITION_ID));



COMMIT;